# 修复 Match 空状态、投递时间轴黑点规则，并汇报进度

## 1. Match 页显示错乱（图 1、图 2）

已定位，不是数据问题，是样式作用域漏了：

`src/pages/Match.tsx` 里有两个返回分支。正常报告分支包在 `<div className="p-match">` 里，而「无报告」分支（第 166–199 行）直接返回 `bgDial + main`，**没有 `.p-match` 包裹**。因为迁移时所有 match 样式都加了 `.p-match` 命名空间前缀，这个分支就等于完全没有 CSS：背景表盘不再被限制成 1400px、`opacity .35`、`fill:none`，于是铺满屏幕变成图 1 的大黑圆；正文也退化成图 2 那样的裸文本。（已在本地用 690px 视口复现，DOM 里确认 `.p-match` 不存在。）

**为什么「明明传了简历和 JD 还是 No report」**：Match 的岗位来源顺序是 `?job=` → `?focus=` → 上次记忆 → 对比池第一个。投递页的「查看匹配」带了 `?focus=<岗位 id>`，所以永远能拿到报告；而岗位画像页解析完 JD 后只调用了 `setUI`，**从没把解析出的岗位写进岗位库**，跳转也是裸 `/match`，Match 拿不到任何 id，只能落到空状态。

修复：

- 空状态分支同样包 `.p-match`，背景表盘恢复正常。
- 岗位画像页解析完成后，把该岗位写入岗位库（仅入库，**不自动进对比池、不自动进投递**），跳转改为 `/match?job=<id>`，让「上传 JD → 看匹配」这条链路自己成立。

## 1.1 岗位流转规则（本次一并对齐）

正确链路：上传简历 → 个人画像；上传 JD → 岗位画像；两者对比 → 匹配报告。之后：

- 只有在 Match 页点「加入对比 →」，岗位才进入对比池；不点不进。
- 只有点「直接投递 →」，岗位才进入投递管理；不点不进。
- 对比池里的岗位可以再点投递进入投递管理。
- 已进入投递管理的岗位**不再支持回到对比池**（二期再做，见文末）。

对应改动：岗位画像解析只做 `putJob`（入库，不入池、不入投递）。

Match 页「显示哪个岗位」的取值顺序也要改。现在是：网址带 `?job=xx` → 网址带 `?focus=xx`（投递页「查看匹配」传的）→ 上次看过的岗位 → **对比池里的第一个岗位**。最后这条是错的：用户刚传完 JD、什么都没点，却会看到对比池里另一个岗位的报告。改为 `?job=` → `?focus=` → 上次分析过的那个岗位；都没有就正常显示空状态，不再兜底去翻对比池。

## 1.2 用户数据隔离

- 数据库层已就绪：`job_profiles` / `match_reports` / `compare_pool` / `applications` / `application_events` 等表均已开启 RLS，策略统一为 `user_id = auth.uid()`，即任何用户只能读写自己的记录，无需本次改动。
- 本次改动只涉及前端页面逻辑，不会新增任何跨用户可读的接口，也不放宽任何策略。
- 现状说明：这几个页面目前仍读写浏览器本地存储（`wfy.ts`），因此数据天然只存在于当前浏览器，不会串号；但换设备也看不到。**把本地存储切换为按登录用户读写数据库**是接真实数据阶段的工作，届时所有读写都经 RLS，隔离由数据库强制保证。




## 2. 重新设计 Match 空状态

沿用 Swiss 风格，不引入新元素：

- 左栏：`01 · OVERVIEW` 标签 + 大字标题「还没有 / 岗位。」+ 一行说明。
- 右栏：把实心月相换成 0.5px 描边空心圆 + 虚线，中间 `AWAITING JD`，与「等待中」的语义一致，不再是一坨黑。
- 下方两个动作按钮改为主次分明：主按钮「上传 JD 建立岗位画像 →」，次按钮「从对比池选一个 →」。
- 窄屏（<900px）单列堆叠，背景表盘降到 `opacity .12`。

## 3. 投递时间轴：只有最新状态是黑色

现在 `src/pages/Delivery.tsx` 第 62 行判定为 `ACTIVE_ON.indexOf(ev.status) > -1 && a.status === ev.status`，写死了只有「已投递」「面试中」两种状态能点亮。所以切到「待投递」或「结束」时，新增的那条在时间轴上仍是灰的。

改为：每条投递记录的**最新一条事件**（时间最晚的那条）为黑色，其余一律灰色，与状态文字无关。删除 `ACTIVE_ON` 白名单。文字与圆点同时变黑（`.dot.on` 已有样式，不动 CSS）。

## 4. 进度汇报

改完后在对话里回答第 3 个问题：已完成 / 剩余 / 下一步，不写进代码。

## 技术说明

- 改动文件：`src/pages/Match.tsx`（空状态包裹 + 重设计 + 岗位回退顺序）、`src/styles/pages/match.css`（空状态与窄屏样式）、`src/pages/JobProfile.tsx`（解析后仅落库并带 id 跳转）、`src/pages/Delivery.tsx`（时间轴点亮规则）。
- 不改任何页面的既有布局与跳转入口，`wfy.ts` 仅使用现有的 `putJob` / `addToPool` / `applyToJob`；「加入对比」「直接投递」仍是唯一的入池、入投递入口。

## 二期计划（本次不做）

- 已进入投递管理的岗位重新回到对比池（需要处理状态回退、重复记录合并与时间轴事件语义）。

