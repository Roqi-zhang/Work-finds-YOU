# Frontend (safe to expose — publishable keys only)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
VITE_SUPABASE_PROJECT_ID=

# Server-side only — configured as Edge Function secrets, never committed
# LOVABLE_API_KEY=        # AI gateway key, injected by the platform
# CUSTOM_AI_ENDPOINT=     # optional: self-hosted / third-party OpenAI-compatible endpoint
# CUSTOM_AI_KEY=          # optional: API key for the endpoint above
# CUSTOM_AI_MODEL=        # optional: override the default model id
